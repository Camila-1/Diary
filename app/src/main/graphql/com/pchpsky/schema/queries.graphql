
query CurrentUser {
    currentUser {
        email
    }
}

mutation CreateUser($email: String!, $password: String!) {
    session: createUser(email: $email, password: $password) {
        token
        user {
            email
        }
    }
}
