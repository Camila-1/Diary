
query CurrentUser {
    currentUser {
        email
    }
}

mutation CreateUser($email: String!, $password: String!) {
    user: createSession(email: $email, password: $password) {
        user {
            email
        }
    }
}
